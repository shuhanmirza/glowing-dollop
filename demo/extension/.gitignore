# Build artifacts produced by wasm/build.sh. Run the script to regenerate them.
pkt.wasm
wasm_exec.js

# Generated by Chrome when loading the unpacked extension: the compiled
# declarativeNetRequest ruleset (and verified_contents.json for store builds).
# Regenerated on every load; not part of the source.
_metadata/
